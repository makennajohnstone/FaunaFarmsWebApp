import React, { Component } from 'react';
import '../css/Meats.css';

//why isn't it rendering the meats page?!
class Meats extends Component {
  render() {
    return (
      <h1> This is the meats page! </h1>
    );
  }
}

export default Meats;
